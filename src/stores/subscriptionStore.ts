// Store placeholder — будет реализован при создании UI
// Используем React hooks + localStorage вместо внешнего state manager
// Основная логика в hooks/useSubscriptions.ts и hooks/useCategories.ts

export {};
